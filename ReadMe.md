The API currently has three endpoints that perform the following
* Insertion sort
* Breadth First Search
* Depth First Search

The output is each step of the algorithm which can be connected to a front-end to display
the changes.